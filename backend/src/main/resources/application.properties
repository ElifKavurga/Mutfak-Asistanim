spring.application.name=mutfak_asistanim
server.port=${PORT:${SERVER_PORT:8081}}
spring.datasource.url=${DB_URL:${SPRING_DATASOURCE_URL:jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:postgres}}}
spring.jpa.properties.hibernate.default_schema=${DB_SCHEMA:kitchen_assistant}
spring.datasource.username=${DB_USERNAME:${PGUSER:postgres}}
spring.datasource.password=${DB_PASSWORD:${PGPASSWORD:238470}}
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:10}
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}
spring.jpa.open-in-view=false
app.jwt.secret=${JWT_SECRET:SjRh3hRRCHAH8+mQjGrRuCXJUsbXckukOimZ+eDJ2fA=}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:7200000}
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:*,http://127.0.0.1:*,http://10.0.2.2:*,http://192.168.*.*:*,https://*.up.railway.app,https://*.railway.app}
